<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dev_fund">

    <!-- ============================================================= -->
    <!-- 1. INTERNET PERMISSIONS (required for Firebase)            -->
    <!-- ============================================================= -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ============================================================= -->
    <!-- 2. OPTIONAL: Play Integrity (App Check) – only needed if you -->
    <!--      activated Play Integrity in the console.                -->
    <!-- ============================================================= -->
    <uses-permission android:name="com.google.android.gms.permission.BIND_PLAY_INTEGRITY_SERVICE" />

    <application
        android:name="${applicationName}"
        android:label="dev_fund"
        android:icon="@mipmap/ic_launcher">

        <!-- ============================================================= -->
        <!-- MainActivity – keep everything Flutter needs                -->
        <!-- ============================================================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============================================================= -->
        <!-- Flutter embedding version (must stay 2)                     -->
        <!-- ============================================================= -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ============================================================= -->
        <!-- OPTIONAL: Firebase App Check – Play Integrity provider      -->
        <!-- ============================================================= -->
        <meta-data
            android:name="com.google.firebase.appcheck.playintegrity.DebugSecret"
            android:value="YOUR_DEBUG_SECRET_IF_USING_DEBUG_PROVIDER" />
        <!-- Remove the line above in release builds -->

    </application>

    <!-- ============================================================= -->
    <!-- 3. QUERIES – keep if you use Android 11+ intent resolution  -->
    <!-- ============================================================= -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

</manifest>